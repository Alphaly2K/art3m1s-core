# art3m1s-core 0.2.2

0.2.2 是面向游戏兼容性和画面一致性的修复版本，配套前端版本为
`Art3m1s 1.1.2-0.2.2c`。

## 主要变化

- **消息文本生命周期**：消息层现在遵循逻辑父图层的显示、隐藏和删除状态。打开其他
  `mw` 页面、返回标题或进入转场时，不再把旧剧情文字留在画面或转场源帧中。
- **图层组透明度**：intermediate-render 图层先合成子树，再统一应用组级 alpha、颜色、
  mask、shader 和混合模式，避免人物脸部、眼睛等重叠部件被重复透明化。
- **实时图层查询**：`get_layer_info` 返回缓动中的当前位置，并能从纹理取得缺省宽高。
  依赖当前位置判断展开/收回的侧边浮窗可以按脚本正常工作。
- **文本排版**：补齐 `spacetop`、`spacemiddle`、`spacebottom`、Ruby 行高、换行后逐行
  对齐和字距处理，减少与原引擎在文字位置和间距上的偏差。
- **跳过速度**：移除每页固定三帧停顿；当前页完整揭示一帧后即可进入下一页。
- **分段 BGM**：core 会识别 Artemis 常见的 `*_a` 引导段和 `*_b` 循环段，并把两段
  路径交给宿主建立无缝播放列表。
- **异步翻译回填**：长译文沿用原文已揭示进度继续播放，不再因原文字数较短而让后半段
  永久保持透明。
- **事件与脚本兼容**：事件过滤器现在获得完整注册参数，并正确处理假成功/假失败结果；
  同时修正若干预处理器、系统变量和消息层恢复细节。

## 升级说明

- FFI 接口保持兼容；建议与 `Art3m1s 1.1.2-0.2.2c` 一起更新。
- iOS 继续使用 Luau 后端，其他平台继续使用 Lua 5.1 后端。
- HLSL 与 E-Mote 仍属于按已观察游戏逐步扩展的兼容实现，并非通用 HLSL/E-Mote SDK。
