[package]
name = "asb-interpreter"
version = "0.2.0"
edition = "2024"

[features]
default = ["backend-lua51"]
backend-lua51 = ["mlua/lua51"]
backend-luau = ["mlua/luau"]

[dependencies]
mlua = { version = "0.10", default-features = false, features = ["send", "serialize", "vendored"] }